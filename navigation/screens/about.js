import React from 'react';
import { Text, View, Image, StyleSheet, Button, ToastAndroid, TouchableOpacity, FlatList} from 'react-native';
import * as Clipboard from 'expo-clipboard';
import { style } from '../styles';

export default function Inicio({navigation, history}) {
  
  return (
    <View style={style.container}>

    </View>
  );
}